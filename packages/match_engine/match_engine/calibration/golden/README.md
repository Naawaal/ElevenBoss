# Golden Match Corpus lives here (JSON fixtures).
# See specs/041-match-engine-v3/quickstart.md and FR-017.
#
# Each fixture: inputs (seed, squads, match type) + parity class + digests.
#
# Offline Dixon-Coles: `dixon_coles_harness.py` (never Discord).
# Runner: `run_corpus.py` / `tests/test_nss_v3_golden_corpus.py`

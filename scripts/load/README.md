# scripts/load/README.md
# Load scripts (050)

Call hub/RPC helpers against **staging/dev** data planes only.

Do **not** use the production Discord gateway as a load generator.

Scaffold scripts land in later tasks (`leaderboard_read.py`, `marketplace_browse.py`, …).
